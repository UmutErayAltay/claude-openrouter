// Filled in fully in a later step; this keeps server/index.ts's wiring final
// while the page itself is still being built.
export function buildDashboardHtml(): string {
  return "<!doctype html><html><body>claude-openrouter dashboard</body></html>";
}
